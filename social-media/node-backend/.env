PORT=5002
MONGO_URI=mongodb://localhost:27017/social-media-node

Connected to MySQL database!

### Step 4: Update Environment Variables (Optional but Recommended)

If you use a `.env` file for your database credentials, update it with your MySQL details.

**File to modify:** <mcfile name=".env" path="c:\Users\sugur\OneDrive\Desktop\social_community_platform\social-media\node-backend\.env"></mcfile> (if it exists, otherwise create it)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=socialmedia